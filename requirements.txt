# Core dependencies for DeepSense Video Scene Analyzer
numpy>=1.26
pandas>=2.0
pyyaml>=6.0
pydantic>=2.5
psutil>=5.9
matplotlib>=3.8
# OpenCV is used by both the pipeline and the YOLO backend.
opencv-python>=4.9
streamlit>=1.32
pytest>=7.4

# Semantic object detection. Without these packages the pipeline falls back to
# the low-accuracy classical motion detector.
torch>=2.2
torchvision>=0.17
ultralytics>=8.1
